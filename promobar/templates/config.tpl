<div class="form-group">
    <label for="promoText">Promo Text:</label>
    <textarea name="promoText" class="form-control">{$promoText}</textarea>
</div>
<div class="form-group">
    <label for="bgColor">Background Color:</label>
    <input type="color" name="bgColor" class="form-control" value="{$bgColor}">
</div>
<div class="form-group">
    <label for="textColor">Text Color:</label>
    <input type="color" name="textColor" class="form-control" value="{$textColor}">
</div>
